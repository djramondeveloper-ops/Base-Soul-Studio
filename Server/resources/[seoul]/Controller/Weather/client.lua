if Config.Modules and Config.Modules.Weather == false then return end

-- A sincronização visual já é feita pelo resource oficial `seoul`.
-- Este módulo só adiciona os comandos server-side /time e /weather.
